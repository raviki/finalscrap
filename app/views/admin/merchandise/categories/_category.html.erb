	<tr>
      	<td><%= link_to category.id,  admin_merchandise_category_path(category) %></td>
        <td><%= category.name %></td>
        <td><%= category.image %></td>
        <td><%= category.description %></td>
        <td><%= category.active %></td>
        <td><%= link_to 'Edit', edit_admin_merchandise_category_path(category) %></td>
        <td><%= link_to  (category.active)? 'Deactivate': 'Activate', :controller => admin_merchandise_categories_path, :id => category.id, :action => "toggle_active", :method => :put %> 	
      </tr>