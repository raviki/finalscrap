<tr>
		<td><%= link_to order.id,  admin_fulfillments_order_path(order) %></td>
        <td><%= link_to order.customer_id,  admin_customers_customer_path(order.customer_id) %></td>
        <td><%= order.address_id %></td>
        <td><%= order.voucher_id %></td>
        <td><%= order.payment_id %></td>
        <td><%= order.discount %></td>
        <td><%= order.discount_message %></td>
        <td><%= order.appointment_date %></td>
        <td><%= order.duration_inHrs %></td>
        <td><%= order.active %></td>
        <% if !(defined? product_detail_page) %>
        <td><%= link_to 'Edit', edit_admin_fulfillments_order_path(order) %></td>
        <td><%= link_to  (order.active)? 'Deactivate': 'Activate', :controller => admin_fulfillments_orders_path, :id => order.id, :action => "toggle_active", :method => :put %> 	
      	<% end %>
      </tr>