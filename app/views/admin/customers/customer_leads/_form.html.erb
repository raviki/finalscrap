<%= form_for @customer_lead, :url  => admin_customers_customer_lead_path(@customer_lead),
				:html => {:class => 'prepend-6'} do |f| %>
  <% if @customer_lead.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_lead.errors.count, "error") %> prohibited this customer_lead from being saved:</h2>

      <ul>
      <% @customer_lead.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_name %><br>
    <%= f.text_area :type_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= submit_tag "Update/Create",
                        :name => nil, :class => 'button small blue-button' %>
  </div>
<% end %>
