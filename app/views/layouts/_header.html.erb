
	<script>
   $(document).ready(function() {
   $('.selectpicker').selectpicker();
});
</script>
<script type="text/javascript" charset="utf-8">
		$(function() {
        $("#addcart").bind("ajax:complete",function() {	
          $("#myModal").modal();
          return false;
        })	
      })
</script>

	<div class="header-block navbar">
		<div class="header-bar navbar-inner container">
			
				<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </a>
				<%= link_to image_tag("logo.jpg", :class => "header-logo"),root_url%>
				<div class="nav-collapse collapse">
					<div class="search-bar">
						<ul class="nav">
						<li>
							<%= form_tag(categories_path, :method => 'get', :class => 'navbar-search') do %>
								<div class="input-prepend  span6 visible-desktop input-append">																				
									<%=	select(@categories,"id", Category.all.collect {|p| [ p.name, p.name ] }, { prompt:"All" },{class:"search-select btn  dropdown-toggle"})%>											
									<%= text_field_tag :key, params[:key], :class => "search-query" %>
									<%= submit_tag "Search", :name => nil, :class => "search-submit btn btn-info" %>								
								</div>
								
								<div class="visible-phone hidden-desktop">
									<%=	select(@categories,"id", Category.all.collect {|p| [ p.name, p.id ] }, { prompt:"All" },{class:"btn btn-block"})%>
									<input type="text">
									<button type="submit" class="btn btn-block">Search</button>
								</div>
							<% end %>
							
							
						</li>						
						</ul>
					</div>
						<ul class="nav pull-right">
							<li><%= link_to "Task ", carts_path, :remote => true, :method=>:get , :id => "addcart" %></li>

							<% if signed_in? %>
            <li id="fat-menu" class="dropdown">
              <a id="sign-btn" href="#" class="dropdown-toggle" data-toggle="dropdown">	
				Hello <%= @user.first_name %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Account", accounts_index_path %></li>
                <li><%= link_to "Orders", orders_path %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", log_out_path%>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in", sessions_path, :id => "sign-btn" %></li>
          <% end %>	
          		</ul>
				</div><!-- /.nav-collapse -->
			
		</div><!-- /navbar-inner -->
	</div><!-- /navbar -->
	

